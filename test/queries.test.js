const { all, search } = require('../src/database/queries');

// all({ year: 2020 }, { offset: 0, limit: 100 }).then(console.log);
// search('ANDIKA TAUFIK', {}, 0, 100, { exact: true }).then(console.log);
